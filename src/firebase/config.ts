import app, { db, auth, storage, functions, getFirebaseMessaging, firebaseConfig } from '../lib/firebase';

export { app, db, auth, storage, functions, getFirebaseMessaging, firebaseConfig };
export default app;
